apiVersion: v1
kind: Service
metadata:
  name: devops4-service
  labels:
    app: devops4
spec:
  type: NodePort
  selector:
    app: devops4
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
